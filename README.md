para que el servicio pueda funcionar exitosamente se necesita crear una queue manager llamada QM.PRUEBAS 
y dos colas, una llamada QM.LOG y QM.IN.PRUEBA; asi mismo se tiene que 
agregar la queue manager default en el server.conf.yaml donde se va a desplegar el servicio
